---
apiVersion: app.ibm.com/v1alpha1
kind: Channel
metadata:
  name: object-bucket
  namespace: app-entitlement
  labels:
    app: object-bucket
    team: team1
    catagory: webapp
spec:
  type: ObjectBucket
  pathname: "<EXPOSED MINIO URL>"
  secretRef:
    name: object-bucket-secret
